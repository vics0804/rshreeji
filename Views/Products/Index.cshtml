@model List<RShreeji.ViewModels.clsProductsViewModel.clsProducts>
@inject Microsoft.Extensions.Options.IOptions<SystemSettings> SysSetting
@using RShreeji.Models.Common;
@{
    ViewData["Title"] = SysSetting.Value.appShortName + " - Products";
    ViewData["MetaKeywords"] = "";
    ViewData["MetaDescription"] = "";
    string strProductsJson = ViewBag.ProductsJson;
}

<!-- Page Header Start -->
<div class="page-header">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h2>Products</h2>
            </div>
            <div class="col-12">
                <p>We are also grinding spices (Masala) at wholesale price.</p>
            </div>
        </div>
    </div>
</div>
<!-- Page Header End -->
<!-- Team Start -->
@if (Model?.Count > 0)
{
    <div class="team">
        <div class="container">
            <div class="section-header text-center wow zoomIn" data-wow-delay="0.1s">
                <p>Specialities</p>
                <h2>Our Handiwork</h2>
            </div>
            <div class="row" id="divProducts">
                @*@foreach (var item in Model)
                    {
                        <div class="col-lg-3 col-md-6 wow fadeInUp" data-wow-delay="0.0s">
                            <div class="team-item px-0 pt-0">
                                <div class="team-img">
                                    <img src="@(FieldNames.ImageFolder + item.thumbimgpath)" alt="@item.description" onerror="loanNoImage(this);">
                                </div>
                                <div class="team-text">
                                    <h2>@item.description</h2>
                                    <p>@item.subdescription</p>
                                </div>
                            </div>
                        </div>
                    }*@
            </div>
        </div>
    </div>
    <!-- Team End -->
}

@section ScriptsBottom {
    <script>

        $(document).ready(function () {
            loadProducts('@strProductsJson');
        });

    </script>
}